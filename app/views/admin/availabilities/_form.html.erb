<%= form_for [:admin, @availability] do |form| %>
  <% if @availability.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(availability.errors.count, "error") %> prohibited this availability model from being saved:</h2>

      <ul>
        <% @availability.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.date_field :date, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= form.time_field :start_time, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= form.time_field :finish_time, class: "form-control" %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
